# Catio V2 — Build Log

Session journal. **Newest entries at the top.** One entry per working session, appended at session end per the protocol in `CLAUDE.md`. Read the top 2–3 entries at session start before touching anything.

## Entry format
```
### YYYY-MM-DD — Short title
**Done:** what changed in the model/repo this session.
**Decisions:** choices made and the reasoning (this is the part future sessions actually need).
**Open questions:** anything unresolved, ambiguous, or awaiting a human answer.
**Next:** the single best next task, specific enough to be a session prompt.
```

---

### 2026-07-16 — r10: the skywalk horseshoe — E → NE → N → NW → W
**Done:** Six entries complete the marquee §1.2 build at the 55.25 rail datum (all decks top out at 56.75): `skywalk-e-n56` (E N-bay link on rails, 11.6" between the NE plate and a 10.75" hop to T1), `corner-ne-56` and `corner-nw-56` (the corner-plate builder grew `facing` — the SE-style sub-group rotates 90°/180°), `skywalk-n-56` and `skywalk-w-56` (new `buildHousewalk`: **2x12 cedar on 2x2 ledgers lagged into house studs**, steel angle brackets every ≤28", `gap` param skips ledger+brackets across the window so the N run **free-spans the glass for 31"** — inside §1.2's 24–32" bracket envelope and the reason those legs are 2x12), and `tread-w-41` (the far-end exit: 40.75 tread, down 11.75 to the chaise back → flap world). The S leg stays open **by design**: a full-height door occupies the S wall in both variants, so no plank can ever cross it — the horseshoe is the maximal skywalk. Verified: node --check clean, zero console errors, dump coordinates exact (plate quaternions decode to 0°/90°/180°), clearance sweep clean, plan-view render shows the full loop. Snapshot `revisions/catio-v2-r9-branch-tree-skywalk-pad.html`; HUD → R10; README updated.
**Decisions:** (1) **The roof call was never the real blocker.** Every roof candidate sits at/above the 80" panel tops (solid-roof rafters dip to ~73" worst case), so the 56.75 datum keeps ≥ 14" headroom under all of them — 22.5" under the leading wire-ceiling candidate. Only ceiling-*anchored* items remain roof-blocked. Logged as the new rule. (2) **House studs are legal mounting substrate** — §1.1's own "into real framing" language, and the window/flap already integrate the house; knock-down is about the panels, which stay independent (ledgers are house-side). (3) The N run crosses the window as a feature: the board occupies a 2¼" band (55.25–57.5) low in the upper pane — cats parade past the kitchen glass; 15" of pane above stays clear. No ledger, brackets, or braces touch glass (NW plate runs `braces: 1`, wing-wall side only). (4) The W run ends at z 66, 12" clear of the W-corner door approach; its south exit is the 40.75 tread (14.5" clearance under the covered stretch, 4" pop lane past the run's end — the TR46 pattern) dropping to the chaise back, which ties the horseshoe's far tip into the lounge layer. (5) The E N-bay link runs z 18.4–30 so NS2 keeps its pop-up lane (z 30–34) and its landing zone keeps 14.5" headroom; the 10.75" level gap to T1 is an easy hop, and boards never cross panel stiles. (6) Bulge spacing: three 18" corner plates + the 14.75" S pad every ≤ 6.5 ft of the ~210" horseshoe — §1.2's passing-section rule met by corners, per its own "make corners generous" guidance.
**Open questions:** Carried: roof call (now only ceiling-anchored extras); door position; over-swing nit; V1 ghost; sun-deck siting; second cubby; locust sourcing. New: does the window's upper sash still open with the parade band outside it? (measure the real sash travel — the model says the band sits at 55.25–57.5, below the sash meeting rail, but verify on the actual window before lagging the ledger).
**Next:** Ground layer: stump forest (§1.4) + scratch log (§4.1) in the west floor block (x 32–44) — natural stairs toward the tree stubs, scent posts, and the horseshoe's under-deck floor circuit. Then sun-map the SE corner plates (§3.3) some sunny morning.

### 2026-07-16 — r9: branch tree + skywalk pad — Phase 2 begins, spur unblocked
**Done:** Two phase-2 entries. (1) `branch-tree` (§1.3, new `buildTree`): 5" tapered bark-on black locust trunk, 62" tall, leaning 11.3° (spec 10–20°) from a 13" ply base plate with lag angle brackets at (45, 62) to a 2x2 tie on the S-wall 55.25 rail — the freestanding/knee-braced variant, zero ceiling dependency. Two branch-stub paw-holds at 30" and 46". The lean is aimed so the trunk surface passes ~1" off the skywalk pad's west edge at deck height (56.75) — a step-across junction. (2) `skywalk-s-56`: the twice-deferred S-wall segment, now legal — 17.25 × **14.75"** pad (doubles as the §1.2 passing bulge for the ~54" T1 + plate + pad L-run), on the 55.25 rails, lipped, x 52–69.25 abutting the corner plate. Its two exits sit at opposite ends: corner plate east (level, low-effort) and tree trunk west. Verified: node --check clean, zero console errors, dump coordinates exact (trunk length 63.22 = √(4.5² + 62² + 11.5²), pad x 52–69.25 top 56.75), collision sweep clean (trunk stays west of ramp/cubby/corridor at every height; pad clears the 36.5 tread below by 19.5"). Snapshot `revisions/catio-v2-r8-winter-cubby-island.html`; HUD → R9; README updated.
**Decisions:** (1) Tree base at (45, 62) — the one floor spot that clears the cubby (4.4" gap), the ramp (trunk surface ≤ x 51.5, ramp starts x 52), the cat corridor, and both door zones, while the 11.3° lean still delivers the trunk to the pad junction; the base sits in the middle-door variant's x-column but 16"+ north of the threshold with an 18"+ passage west of the trunk — a walk-around post, not a blockage. (2) Top anchor = rail tie, not ceiling: load path is floor plate + S rail ("floor or spread across rails"), knock-down by unbolting the tie. (3) Pad depth 14.75" (1x12 + ripped sister board IRL) so the §1.2 bulge requirement lands with the segment; §1.2's 2x12 decking spec is deferred to the full perimeter, where 24–32" bracket spans on the house walls actually demand it — this segment spans rails like the rest of the top-run family. (4) Trunk tip masts 5.25" above the pad deck at its west edge — intentional grab post at the junction, headroom above it 18". (5) The tree is bonus highway, not a rated route: senior access to the pad stays level via the corner plate; the trunk is the standard-effort second exit.
**Open questions:** Carried: roof call (still gates the N/W skywalk perimeter — house-wall mounting is its own question); door position; door over-swing nit; V1-ghost interpenetration; sun-deck siting; second cubby. New: real-tree sourcing — a 62" straight-enough locust limb with a usable taper; §1.3 prep (dry weeks, knock bark loose spots) starts whenever one turns up.
**Next:** Stump forest (§1.4) + scratch log (§4.1) as floor entries in the west block (x 32–44, z 36–60 remains open) — natural stairs to the tree stubs and the scent-post layer. Or, if the roof call lands first: the N/W skywalk perimeter design session.

### 2026-07-16 — r8: winter cubby island — Phase 1 complete
**Done:** Added the §3.4 winter cubby as a freestanding island (`cubby-c`, new `buildCubby` builder): 20×20" interior on PT 4x4 skids, floor raised to 4¼", interior height 16" at the low back wall (Hobie spec-min) rising to 18½" at the door face, cedar shed roof at 6.6° shedding north, 7" door hole facing S — away from the N/NW prevailing wind, opening onto the sheltered 8.5" corridor between cubby and ramp. The roof doubles as a route node: sofa seat (14) → roof (21–23.5, +7") → stair tread 17.5 (−4/−6") or shelf 26.25 (+3/+4.6") — a senior-grade shortcut from the flap world onto the stairs that bypasses the floor ramp. Roof has four exits. Catalog Phase 1 (shelf runs, senior ramp, corner plate, one cubby) is now **complete**. Verified: node --check clean, zero console errors, all 10 cubby pieces at designed coordinates (roof quaternion = 6.63°), no collisions with ramp/sofa/stair hardware (5–8.5" clearances as designed), audit passes. Snapshot `revisions/catio-v2-r7-apparatus-wall.html`; HUD → R8; README updated.
**Decisions:** (1) **Skywalk first segment (S-wall top run) re-deferred, with reasons this time:** with the corner plate occupying x 69.25–87.25, the segment shrinks to x 52–69.25 and its only legal exit is the plate itself — below is a 20¼" drop (over the 16" hop max), west is door territory in both variants. Still a single-exit spur. It unblocks when any of these land: the §1.3 branch tree (freestanding variant) gives the SE quadrant a mid-room vertical, the roof call enables the N/W perimeter, or the door-position call frees the S-run wire panel for a step-down. (2) **Cubby placement — center island, not E wall:** the E-wall floor windows between sofa, stair tread 17.5 (z 42–56 + hangers), and ramp landing legs are all under 11" — the apparatus wall did its job; no 21.5" box fits there. The island sits clear of both door paths and the human center path, and turns dead floor into a route node. (3) Single-wall ply now; the §3.4 winter double-wall (enclosed foam sandwich) is a later upgrade to the same footprint. (4) Door hole modeled as trim ring + dark disc — established model language for openings. (5) Straw bedding per catalog (not modeled). (6) Human access to the SE corner is a crouch/reach zone since r7's shelving — the island narrows it but the low ramp foot (2–4" deck) is step-over-able; main door→center paths stay clear in both variants.
**Open questions:** Carried: roof call (now gates skywalk perimeter AND branch-tree top anchor); door position; door over-swing nit; V1-ghost interpenetration; sun-deck siting on the corner plate. New: §3.4's two-cats-one-rule — if Hobie and Zoey both claim the cubby, a second one needs a spot (SW corner pends the door call).
**Next:** The roof call is a human decision and now the critical path. Model-side, the best unblocked task: §1.4 stump forest / §4.1 scratching infrastructure as floor-standing APPARATUS entries in the remaining open floor (x 32–52 west block), or the §1.3 branch tree in its freestanding/knee-braced variant (floor plate + rail braces, no ceiling anchor) — which would also finally give the S-wall skywalk segment its second exit.
**Done:** First real apparatus in the manifest: 10 phase-1 entries (catalog §1.1 shelf runs + §1.5 senior ramp + §1.2/§3.2 corner plate) consumed by three new builders — `buildShelf` (mounts: rail / hang / post / leg, faces E/S, optional lip, ply option), `buildCorner`, `buildRamp`. Senior spine floor → top, every rise ≤ 10¼": 20.0° cleated ramp (S wall, x 52–74, low outer rail) → corner landing @ 8¾ → tread @ 17½ → rail shelf @ 26¼ → tread @ 36½ (posts standing on the 24¾ rail) → tread @ 46½ (hung from the 55¼ rail, pop-up lane past the plate) → corner plate @ 56¾ → E-wall top run (lipped). Standard fast lane, N bay: seat 14 → 26¼ → 41½ → top run (hops 12¼–15¼). Flap link: sofa back (29) walks down onto the N-bay shelf @ 26¼, which links level (16") to the S-bay run. Verified: `node --check` clean; zero page console output; scene-dump coordinates all match design (ramp quaternion decodes to 19.98°); audit passes — gaps ≤ 16", top-run headroom 23¼", ≥ 2 exits per element, senior route continuous, both door variants + head path clear, no mesh-mounting, knock-down preserved. Snapshot `revisions/catio-v2-r6-apparatus-data-layer.html`; HUD → R7; README plan + revisions updated; `.gitignore` added (.DS_Store).
**Decisions:** (1) **S-wall top run cut** — alone it is a single-exit spur (trap rule); it becomes the Phase-2 skywalk's first segment, where it gains neighbors and exits organically. (2) **Straight ramp + ≤ 10¼" stairs instead of a literal switchback ramp:** the ~35" wall segments can't fit two 20° legs plus landings without entering the middle-door zone; every §1.5 hard number (18–20°, 4" cleats, low rail, level top landing) is met, with the 12×18 landing spec read as the ply landing + level ramp-crest continuum (~12.5 × 19 staging). (3) **Corner plates** abut both wall faces at rail-top height with V1-style knee braces and lift off — treated as knock-down-legal since nothing fastens across panels. (4) **30.5" mid-rail consequence resolved:** S bay gets two fine steps (36½ / 46½), N bay one (41½). (5) Rises grow slightly with height (8¾ low → 10¼ high) — mildly inverse of higher-easier, but every step is senior-rated; forced by the fixed 26¼ rail datum. (6) NS1↔S1 16" level link rated standard; seniors reach the spine via the floor ramp or the flap walk-off.
**Open questions:** Carried: roof call; door position; door over-swing nit (−1.9 rad through the wall plane). New: (a) "Show V1 interior" ghost now interpenetrates the new top-run zone when toggled — stowed scenery, relocate or accept? (b) Corner plate @ 56¾ is the winter-sun perch candidate (S/SE exposure) — confirm by observation before siting the §3.3 sun deck there.
**Next:** Phase 2 opener: perimeter skywalk (§1.2) on the 55¼" rail datum — first segment is the deferred S-wall top run (x 52–86½ @ 56¾ + a 14"+ passing bulge), which gives the corner plate a third neighbor. Headroom works under the TBD 80" ceiling (23¼"), but get the roof call before committing the full perimeter.

### 2026-07-16 — r6: APPARATUS data layer (behavior-preserving refactor)
**Done:** Interior is now data-driven per the architecture target: `APPARATUS` config array (`{id, type, name, pos, dims, phase, built}`) + per-type builders (`buildSectional`, `buildChaise`) + a `BUILDERS` dispatch loop. First entries: `sectional-north` and `chaise-west` (phase 0, `built: false`), migrated out of the inline "V2 INTERIOR" block; their three materials moved into the shared `M` dict (`secFrame`/`secCushion`/`secBack`). Scenery (stowed V1 interior, cats, compass, dimension labels) stays inline. Housekeeping: catalog moved to `docs/catio-v2-apparatus-catalog.md` (repo map said `docs/`, file sat at root); snapshot `revisions/catio-v2-r5-full-north-lounge.html`; HUD → DRAFT R6; CLAUDE.md architecture section rewritten to the landed state; README revision list → r1–r5. Verification (headless Chrome/SwiftShader, pre vs post): scene-graph dumps — 274 objects, world transforms + geometry params + colors + shadow flags — byte-identical at rest *and* mid-animation with the door open; 15 toggle/view screenshot states all identical except the intended R5→R6 HUD digit, with two explained deltas: flap = capture jitter (its sine wobble never settles; same-file reruns jitter equally) and door-open = a 38×193 px draw-order sliver, bisect-proven to come solely from material creation order (three.js sorts opaque draws by material id) flipping the depth-tie winner where the open leaf interpenetrates the south house-wall plane — an artifact already present in r5. `node --check` clean; zero page console output.
**Decisions:** (1) Two manifest entries, not one L-shaped blob — the W-wall back (z 2–56) belongs to `chaise-west` and shares the NW corner behind the sectional's corner seat; the split is bookkeeping, geometry unchanged. (2) `pos` = west/floor/north min corner, `dims` = `{w, d, h}` bounding box, world inches — matches tape-measure thinking. (3) Envelope-derived config only where the envelope is the constraint: sectional `w: XE - 4` (full N wall less 2" clearance per end), chaise `d: L - 24` so the foot keeps the documented 22" gap to the S-wall door. (4) `phase: 0` = furniture/layout; catalog phases 1–3 reserved for apparatus. (5) `built: false` renders solid for now — ghosting planned-vs-built is a future display decision; zero-visual-change ruled it out this session.
**Open questions:** Carried: roof call; door position W corner vs middle. New (cosmetic, pre-existing in r5): the W-corner door leaf opens −1.9 rad (≈109°), swinging through the house's south-wall plane at full open — cap the swing near −1.5 rad or add a doorstop in a future session if it grates.
**Next:** The apparatus wall (E face + SE corner) as APPARATUS entries: catalog §1.1 cedar shelf runs + §1.5 senior switchback ramp. Constraint to design around: the 30.5" gap between the mid rails (24.75 → 55.25) exceeds hop spec — every climbing bay needs one intermediate step (cleat-mounted between rails, or offset into the adjacent bay).

### 2026-07-16 — Docs added; repo reviewed
**Done:** Added `CLAUDE.md`, `docs/catio-v2-apparatus-catalog.md`, and this log. Reviewed `v2.html` (draft r5) and README; CLAUDE.md's repo map, model conventions, and envelope facts were filled in from source. Layout/structure decisions before this log live in commit history and `revisions/`.

**Decisions:** Session protocol per `CLAUDE.md` (one task / audit / log / snapshot / commit). Apparatus specs defer to the catalog. The `APPARATUS` data-layer refactor is required **before** apparatus-wall work begins — the interior is currently inline `addBox` calls (see ~line 557).

**Open questions:** (1) Roof call — flat wire ceiling at 80" is the leading candidate; blocks ceiling-dependent apparatus (branch-tree top anchor, hanging items). (2) Door position, W corner vs middle — affects SE-corner apparatus clearances; the chaise foot already constrains a 22" gap to the door. (3) Confirm the skywalk rides the 55.25" rail datum (~24" headroom under an 80" ceiling) once the roof lands.

**Next:** `APPARATUS` refactor session — introduce the config array + per-type builder functions per CLAUDE.md's Architecture target; migrate the sectional/chaise into it as the first entries; run the audit; snapshot as r6 if it lands clean. After that: the apparatus wall (catalog §1.1 shelf runs + §1.5 senior ramp), remembering the 30.5" mid-rail gap needs one intermediate step per climbing bay.
